import React from 'react';
import { Message } from '../components';

const TPSStats = () => (
  <div class="container">
    <div class="row">
      <div class="col-xs-12">
        <Message
          text="Statistik"
          subText="Akan datang..."
        />
      </div>
    </div>
  </div>
);

export default TPSStats;
